MOG TODOs

- Add `--output` flag to CLI to control output directory or filename for generated reports.
- Implement XLSX chart writer for new `chart` element (bar, line, area, pie, scatter; stacked and data labels options).
- Add sample report demonstrating a chart bound to a table.

